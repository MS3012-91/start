import React from 'react'

export default function LoadingUsers()


async function load ({results, page, seed}) {
    await fetch (' https://randomuser.me/api/?results=${reselts}&seed={seed}&page={page} ')
}
{
  return (
    <div>

    </div>
  )
}
